device: "auto"          # "auto" | "cpu" | "cuda"
seed: 42
deterministic: true

epochs: 5
lr: 3e-4
weight_decay: 1e-2
grad_clip_norm: 1.0
amp: true

log_every_n_steps: 20
val_every_n_steps: 200

checkpoint:
  save_dirname: "checkpoints"
  save_last: true
